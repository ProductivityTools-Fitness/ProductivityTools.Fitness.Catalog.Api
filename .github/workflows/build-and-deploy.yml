name: Build and Deploy

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # Allow manual trigger

jobs:
  build-and-deploy:
    strategy:
      fail-fast: false # Pozwala jednemu runnerowi dokończyć wdrożenie nawet jeśli drugi napotka błąd
      matrix:
        include:
          - target-runner: "fitness-catalog-api"
            server-port: "8085"
            db-url: "jdbc:postgresql://10.10.0.2:5432/ptfitness-catalog"
            db-user: "postgres"
          - target-runner: "T460Ubuntu1"
            server-port: "8085"
            db-url: "jdbc:postgresql://localhost:5432/ptfitness-catalog"
            db-user: "postgres"

    runs-on: [ self-hosted, "${{ matrix.target-runner }}" ]

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    # Identify user running the workflow
    - name: Identify User
      run: |
        echo "Running on runner: ${{ matrix.target-runner }}"
        whoami
        id

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'gradle'

    - name: Build with Gradle
      env:
        SERVER_PORT: ${{ matrix.server-port }}
        DB_URL: ${{ matrix.db-url }}
        DB_USERNAME: ${{ matrix.db-user }}
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      run: |
        chmod +x gradlew
        ./gradlew --stop
        ./gradlew clean build --rerun-tasks

    - name: Stop application
      run: |
        echo "Stopping application"
        sudo systemctl stop fitness-catalog-api || true

    - name: Copy Jar to /opt/fitness-catalog-api
      run: |
        echo "Copying JAR to /opt/fitness-catalog-api"
        umask 002
        JAR=build/libs/fitness-catalog-api-0.0.1-SNAPSHOT.jar
        if [ ! -f "$JAR" ]; then
          echo "Jar not found at $JAR"; exit 1
        fi
        sudo mkdir -p /opt/fitness-catalog-api
        sudo cp "$JAR" /opt/fitness-catalog-api/

    - name: Configure environment variables and systemd service
      env:
        SERVER_PORT: ${{ matrix.server-port }}
        DB_URL: ${{ matrix.db-url }}
        DB_USER: ${{ matrix.db-user }}
        DB_PASS: ${{ secrets.DB_PASSWORD }}
      run: |
        echo "Configuring environment variables and systemd service file..."
        
        # 1. Create secure environment file with runner-specific configuration
        cat << EOF > /tmp/fitness-catalog-api.env
        SERVER_PORT=$SERVER_PORT
        DB_URL=$DB_URL
        DB_USERNAME=$DB_USER
        EOF

        if [ -n "$DB_PASS" ]; then
          echo "DB_PASSWORD=$DB_PASS" >> /tmp/fitness-catalog-api.env
        fi

        sudo mv /tmp/fitness-catalog-api.env /opt/fitness-catalog-api/fitness-catalog-api.env
        sudo chmod 600 /opt/fitness-catalog-api/fitness-catalog-api.env

        # 2. Configure and install systemd service
        CURRENT_USER=$(whoami)
        cp deployment/fitness-catalog-api.service /tmp/fitness-catalog-api.service
        sed -i "s/USER_PLACEHOLDER/$CURRENT_USER/g" /tmp/fitness-catalog-api.service
        sudo mv /tmp/fitness-catalog-api.service /etc/systemd/system/fitness-catalog-api.service
        sudo systemctl daemon-reload

    - name: Start application
      run: |
        echo "Starting application"
        sudo systemctl start fitness-catalog-api
